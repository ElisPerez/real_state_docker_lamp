-- Inicia con...
   SELECT *
     FROM services
    WHERE name LIKE 'Corte%';

-- Finaliza con...
   SELECT *
     FROM services
    WHERE name LIKE '%Cabello';

-- Donde sea que lo encuentres
   SELECT *
     FROM services
    WHERE name LIKE '%de%';